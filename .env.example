# Environment Configuration Template
# Copy this file to .env and replace placeholder values with your actual secrets
#
# SECURITY WARNING: Never commit the actual .env file to version control!
# The .env file should contain real secrets and is excluded by .gitignore

# API Authentication Key
# Generate a secure random string for production use
# This key will have 5 credits and is used to authenticate API requests
API_KEY=your_secret_api_key_here

# Example of generating a secure key (run this in terminal):
# python -c "import secrets; print(secrets.token_urlsafe(32))"
